export class Hotel {
  // name, city, country, add
  constructor(
    private id: string,
    public name: string,
    public city: string,
    public country: string,
    public address: string
  ) {}
}
